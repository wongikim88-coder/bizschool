# BIZSCHOOL 메인 페이지 Design Document

> **Summary**: 인프런 벤치마킹 기반 BIZSCHOOL 메인 페이지 컴포넌트 설계 및 구현 명세
>
> **Project**: BIZSCHOOL
> **Version**: 0.1.0
> **Author**: Allen
> **Date**: 2026-02-25
> **Status**: Draft
> **Planning Doc**: [bizschool-main.plan.md](../../01-plan/features/bizschool-main.plan.md)

---

## 1. Overview

### 1.1 Design Goals

- 인프런(inflearn.com)과 동일한 수준의 UI/UX 품질 달성
- BIZSCHOOL 브랜딩(로고, 색상, 폰트)을 일관성 있게 적용
- 컴포넌트 재사용성 확보 (강의 카드 / 도서 카드 패턴 통일)
- Mobile-first 반응형 레이아웃 구현

### 1.2 Design Principles

- **컴포넌트 분리**: 각 UI 영역을 독립 컴포넌트로 분리하여 재사용성 극대화
- **일관된 스타일링**: Tailwind CSS 커스텀 토큰으로 색상/폰트를 중앙 관리
- **정적 데이터 우선**: 샘플 데이터로 UI 완성 후, 추후 API 연동 확장

---

## 2. Architecture

### 2.1 Component Diagram

```
┌─────────────────────────────────────────────────────────┐
│  app/layout.tsx                                         │
│  ┌───────────────────────────────────────────────────┐  │
│  │  <Header />                                       │  │
│  │  ┌─────────────┬───────────────┬───────────────┐  │  │
│  │  │ Logo        │ Navigation    │ RightActions  │  │  │
│  │  └─────────────┴───────────────┴───────────────┘  │  │
│  ├───────────────────────────────────────────────────┤  │
│  │  <SearchBar />                                    │  │
│  ├───────────────────────────────────────────────────┤  │
│  │                                                   │  │
│  │  app/page.tsx                                     │  │
│  │  ┌─────────────────────────────────────────────┐  │  │
│  │  │ <HeroBanner />                              │  │  │
│  │  ├─────────────────────────────────────────────┤  │  │
│  │  │ <RecommendedCourses />                      │  │  │
│  │  │  └─ <CourseCard /> × 4 (grid)               │  │  │
│  │  ├─────────────────────────────────────────────┤  │  │
│  │  │ <RecommendedBooks />                        │  │  │
│  │  │  └─ <BookCard /> × 4 (grid)                 │  │  │
│  │  └─────────────────────────────────────────────┘  │  │
│  │                                                   │  │
│  ├───────────────────────────────────────────────────┤  │
│  │  <Footer />                                       │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 2.2 Data Flow

```
샘플 데이터 (data/*.ts) → 페이지 컴포넌트 (page.tsx) → 섹션 컴포넌트 → 카드 컴포넌트 → 렌더링
```

### 2.3 Dependencies

| Package | Version | Purpose |
|---------|---------|---------|
| next | ^15 | React 프레임워크 (App Router) |
| react | ^19 | UI 라이브러리 |
| tailwindcss | ^4 | 유틸리티 CSS |
| lucide-react | ^0.460 | 아이콘 라이브러리 |
| @next/font (built-in) | - | Google Fonts 최적화 로딩 |

---

## 3. Data Model

### 3.1 Entity Definition

```typescript
// types/index.ts

/** 강의 데이터 타입 */
export interface Course {
  id: string;
  title: string;            // 강의 제목 (최대 2줄)
  instructor: string;       // 강사명
  thumbnail: string;        // 썸네일 이미지 URL
  price: number;            // 현재 가격
  originalPrice?: number;   // 원래 가격 (할인 시)
  discountRate?: number;    // 할인율 (%)
  rating: number;           // 평점 (0~5)
  reviewCount: number;      // 리뷰 수
  studentCount: number;     // 수강생 수
  badges?: Badge[];         // 뱃지 목록
}

/** 도서 데이터 타입 */
export interface Book {
  id: string;
  title: string;            // 도서 제목 (최대 2줄)
  author: string;           // 저자명
  cover: string;            // 표지 이미지 URL
  price: number;            // 현재 가격
  originalPrice?: number;   // 원래 가격
  discountRate?: number;    // 할인율 (%)
  rating: number;           // 평점 (0~5)
  reviewCount: number;      // 리뷰 수
  badges?: Badge[];         // 뱃지 목록
}

/** 뱃지 타입 */
export interface Badge {
  label: string;            // "New", "베스트셀러", "Update" 등
  variant: 'primary' | 'green' | 'red' | 'blue' | 'gray';
}

/** 메뉴 아이템 타입 */
export interface MenuItem {
  label: string;
  href: string;
}
```

---

## 4. UI/UX 상세 설계

### 4.1 전체 페이지 레이아웃

```
┌──────────────────────────────────── 100vw ────────────────────────────────┐
│ ┌──────────────────────── max-w-[1200px] mx-auto ────────────────────┐   │
│ │                                                                     │   │
│ │  HEADER (sticky top-0, bg-white, border-b, h-16, z-50)            │   │
│ │  ┌──────┬──────────────────────────────────┬──────────────────┐    │   │
│ │  │ LOGO │ NAV: 강의 | 도서 | 전문가상담 | 커뮤니티 │ 전문가지원 로그인 │    │   │
│ │  └──────┴──────────────────────────────────┴──────────────────┘    │   │
│ │                                                                     │   │
│ │  SEARCHBAR (py-4)                                                  │   │
│ │  ┌────────────────────────────────────────────────────────────┐    │   │
│ │  │ 🔍 배우고 싶은 강의를 검색해보세요                            │    │   │
│ │  └────────────────────────────────────────────────────────────┘    │   │
│ │                                                                     │   │
│ ├─────────────────────────────────────────────────────────────────────┤   │
│ │                                                                     │   │
│ │  HERO BANNER (rounded-xl, overflow-hidden, aspect-[21/9])         │   │
│ │  ┌────────────────────────────────────────────────────────────┐    │   │
│ │  │  bg-gradient: #155dfc → #101828                             │    │   │
│ │  │  "BIZSCHOOL에서 비즈니스 역량을 키워보세요"                    │    │   │
│ │  │  "전문가가 직접 전하는 실무 중심의 강의와 도서"                  │    │   │
│ │  │              [무료로 시작하기] (CTA 버튼)                     │    │   │
│ │  └────────────────────────────────────────────────────────────┘    │   │
│ │                                                                     │   │
│ ├─────────────────────────────────────────────────────────────────────┤   │
│ │                                                                     │   │
│ │  SECTION: 추천강의 (py-12)                                         │   │
│ │  ┌────────────────────────────────────────────────────────────┐    │   │
│ │  │ "추천강의" (text-2xl font-bold)            [더보기 →]      │    │   │
│ │  ├────────────────────────────────────────────────────────────┤    │   │
│ │  │ gap-6, grid-cols-1 sm:grid-cols-2 lg:grid-cols-4          │    │   │
│ │  │ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐                      │    │   │
│ │  │ │Course│ │Course│ │Course│ │Course│                      │    │   │
│ │  │ │Card  │ │Card  │ │Card  │ │Card  │                      │    │   │
│ │  │ └──────┘ └──────┘ └──────┘ └──────┘                      │    │   │
│ │  └────────────────────────────────────────────────────────────┘    │   │
│ │                                                                     │   │
│ ├─────────────────────────────────────────────────────────────────────┤   │
│ │                                                                     │   │
│ │  SECTION: 추천도서 (py-12, bg-[#f8f9fa])                           │   │
│ │  ┌────────────────────────────────────────────────────────────┐    │   │
│ │  │ "추천도서" (text-2xl font-bold)            [더보기 →]      │    │   │
│ │  ├────────────────────────────────────────────────────────────┤    │   │
│ │  │ gap-6, grid-cols-1 sm:grid-cols-2 lg:grid-cols-4          │    │   │
│ │  │ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐                      │    │   │
│ │  │ │Book  │ │Book  │ │Book  │ │Book  │                      │    │   │
│ │  │ │Card  │ │Card  │ │Card  │ │Card  │                      │    │   │
│ │  │ └──────┘ └──────┘ └──────┘ └──────┘                      │    │   │
│ │  └────────────────────────────────────────────────────────────┘    │   │
│ │                                                                     │   │
│ ├─────────────────────────────────────────────────────────────────────┤   │
│ │                                                                     │   │
│ │  FOOTER (bg-dark-navy #101828, text-white, py-12)                  │   │
│ │                                                                     │   │
│ └─────────────────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────────────┘
```

### 4.2 Header 컴포넌트 상세

```
┌─────────────────────────────── Header (h-16, border-b border-[#e5e7eb]) ─────────────────┐
│                                                                                           │
│  ┌──────────┐  ┌─────────────────────────────────────┐  ┌─────────────────────────────┐  │
│  │ BIZSCHOOL│  │ 강의    도서    전문가상담    커뮤니티    │  │  🎓 전문가지원   🔑 로그인  │  │
│  │          │  │                                       │  │                             │  │
│  │ font:    │  │ font: Noto Sans KR, 500 weight       │  │  Lucide 아이콘 + 텍스트      │  │
│  │ Squada   │  │ color: #282c34                        │  │  color: #6a7282             │  │
│  │ One      │  │ gap: 32px (gap-8)                     │  │  hover: #282c34             │  │
│  │ color:   │  │ hover: #155dfc (text-blue-primary)    │  │  gap: 24px (gap-6)          │  │
│  │ #282c34  │  │ active: border-b-2 border-blue-primary│  │                             │  │
│  │ text-2xl │  │ text-[15px]                           │  │  text-sm                    │  │
│  └──────────┘  └─────────────────────────────────────┘  └─────────────────────────────┘  │
│                                                                                           │
│  ← flex-shrink-0 →  ← flex-1, justify-center →         ← flex-shrink-0 →                │
│                                                                                           │
└───────────────────────────────────────────────────────────────────────────────────────────┘

Mobile (< 768px):
┌──────────────────────────────┐
│ BIZSCHOOL          [≡] 햄버거 │
│                              │
│ 🔍 검색바                     │
├──────────────────────────────┤
│ 강의 | 도서 | 전문가상담 | 커뮤니티 │  ← 가로 스크롤
└──────────────────────────────┘
```

### 4.3 SearchBar 컴포넌트 상세

```
┌───── SearchBar (max-w-[800px] mx-auto, py-4) ─────┐
│                                                     │
│  ┌─── input container (relative) ──────────────┐   │
│  │                                              │   │
│  │  🔍  배우고 싶은 강의를 검색해보세요            │   │
│  │  ↑                                           │   │
│  │  Search icon (absolute left-4)               │   │
│  │                                              │   │
│  │  bg: #f8f9fa                                 │   │
│  │  border: 1px solid #e5e7eb                   │   │
│  │  border-radius: 28px (rounded-full)          │   │
│  │  padding: 14px 20px 14px 48px                │   │
│  │  font: Noto Sans KR, 14px                    │   │
│  │  placeholder-color: #6a7282                  │   │
│  │  focus: border-blue-primary, ring-2          │   │
│  │                                              │   │
│  └──────────────────────────────────────────────┘   │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### 4.4 HeroBanner 컴포넌트 상세

```
┌───── HeroBanner (rounded-2xl, overflow-hidden, relative) ─────┐
│                                                                 │
│  bg: linear-gradient(135deg, #155dfc 0%, #101828 100%)         │
│  min-height: 280px (desktop), 200px (mobile)                    │
│  padding: 48px                                                  │
│                                                                 │
│  ┌──────────────────────────────────────┐                      │
│  │                                      │                      │
│  │  "BIZSCHOOL에서"                      │  text-white/80       │
│  │  "비즈니스 역량을 키워보세요"           │  text-white          │
│  │                                      │  text-3xl font-bold  │
│  │  "전문가가 직접 전하는 실무 중심의"      │                      │
│  │  "강의와 도서"                         │  text-white/70       │
│  │                                      │  text-lg mt-4        │
│  │  [무료로 시작하기]                     │  mt-8               │
│  │   bg-white text-blue-primary          │  rounded-lg         │
│  │   px-6 py-3 font-semibold             │  hover:bg-gray-100  │
│  │                                      │                      │
│  └──────────────────────────────────────┘                      │
│                                                                 │
│  (우측 하단) 장식 도형: opacity-10 circles                       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4.5 CourseCard 컴포넌트 상세

```
┌───── CourseCard (group, cursor-pointer) ─────┐
│                                               │
│  ┌─── 썸네일 (aspect-video, rounded-lg) ───┐ │
│  │                                          │ │
│  │  <Image />                               │ │
│  │  object-fit: cover                       │ │
│  │  group-hover: brightness-95              │ │
│  │                                          │ │
│  └──────────────────────────────────────────┘ │
│                                               │
│  ┌─── 정보 영역 (pt-3) ────────────────────┐ │
│  │                                          │ │
│  │  강의 제목 (최대 2줄)                      │ │
│  │  text-[15px] font-semibold               │ │
│  │  text-dark (#282c34)                     │ │
│  │  line-clamp-2                            │ │
│  │                                          │ │
│  │  강사명                                   │ │
│  │  text-sm text-muted (#6a7282)            │ │
│  │  mt-1                                    │ │
│  │                                          │ │
│  │  ┌─── 가격 영역 (mt-2) ──────────────┐  │ │
│  │  │ (할인 시)                           │  │ │
│  │  │ ₩55,000  ← line-through text-muted │  │ │
│  │  │ 30% ₩38,500  ← text-red font-bold │  │ │
│  │  │ (정가 시)                           │  │ │
│  │  │ ₩55,000  ← text-dark font-bold    │  │ │
│  │  └────────────────────────────────────┘  │ │
│  │                                          │ │
│  │  ┌─── 메타 영역 (mt-2) ──────────────┐  │ │
│  │  │ ★ 4.8 (45)   👤 600+              │  │ │
│  │  │ text-sm text-muted                 │  │ │
│  │  │ star: text-yellow-400              │  │ │
│  │  └────────────────────────────────────┘  │ │
│  │                                          │ │
│  │  ┌─── 뱃지 영역 (mt-2, flex gap-1) ──┐  │ │
│  │  │ [New] [베스트]                      │  │ │
│  │  │ text-xs px-2 py-0.5 rounded        │  │ │
│  │  └────────────────────────────────────┘  │ │
│  │                                          │ │
│  └──────────────────────────────────────────┘ │
│                                               │
└───────────────────────────────────────────────┘

Badge 색상:
  primary: bg-blue-primary/10 text-blue-primary
  green:   bg-emerald-50 text-emerald-600
  red:     bg-red-50 text-red-500
  blue:    bg-blue-50 text-blue-500
  gray:    bg-gray-100 text-gray-600
```

### 4.6 BookCard 컴포넌트 상세

```
┌───── BookCard (group, cursor-pointer) ───────┐
│                                               │
│  ┌─── 표지 (aspect-[3/4], rounded-lg) ────┐  │
│  │                                         │  │
│  │  <Image />                              │  │
│  │  object-fit: cover                      │  │
│  │  shadow-md                              │  │
│  │  group-hover: shadow-lg scale-[1.02]    │  │
│  │  transition-transform duration-200      │  │
│  │                                         │  │
│  └─────────────────────────────────────────┘  │
│                                               │
│  ┌─── 정보 영역 (pt-3) ────────────────────┐ │
│  │                                          │ │
│  │  도서 제목 (최대 2줄)                      │ │
│  │  text-[15px] font-semibold               │ │
│  │  text-dark (#282c34)                     │ │
│  │  line-clamp-2                            │ │
│  │                                          │ │
│  │  저자명                                   │ │
│  │  text-sm text-muted (#6a7282)            │ │
│  │  mt-1                                    │ │
│  │                                          │ │
│  │  ┌─── 가격 영역 ─────────────────────┐   │ │
│  │  │ (CourseCard와 동일 패턴)            │   │ │
│  │  └────────────────────────────────────┘   │ │
│  │                                          │ │
│  │  ★ 4.5 (30)  ← 평점                     │ │
│  │  [뱃지]  ← 선택적                         │ │
│  │                                          │ │
│  └──────────────────────────────────────────┘ │
│                                               │
└───────────────────────────────────────────────┘
```

### 4.7 Footer 컴포넌트 상세

```
┌───── Footer (bg-[#101828] text-white py-12) ──────────────────────────┐
│                                                                        │
│  ┌── max-w-[1200px] mx-auto ──────────────────────────────────────┐   │
│  │                                                                 │   │
│  │  ┌─── grid grid-cols-2 md:grid-cols-4 gap-8 ───────────────┐  │   │
│  │  │                                                           │  │   │
│  │  │  BIZSCHOOL       강의            도서          고객지원     │  │   │
│  │  │  (Squada One)    ├ 전체강의       ├ 전체도서    ├ FAQ      │  │   │
│  │  │  text-white      ├ 추천강의       ├ 추천도서    ├ 1:1문의  │  │   │
│  │  │  text-xl         ├ 신규강의       ├ 신간도서    ├ 공지사항  │  │   │
│  │  │                  └ 무료강의       └ 베스트셀러  └ 이용약관  │  │   │
│  │  │                                                           │  │   │
│  │  └───────────────────────────────────────────────────────────┘  │   │
│  │                                                                 │   │
│  │  ── border-t border-white/20 mt-8 pt-8 ──                      │   │
│  │                                                                 │   │
│  │  COPYRIGHT 2026 BIZSCHOOL. ALL RIGHTS RESERVED.                │   │
│  │  text-sm text-white/50                                         │   │
│  │                                                                 │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 5. Component Specification

### 5.1 Component List

| Component | File Path | Props | Responsibility |
|-----------|-----------|-------|----------------|
| **Header** | `src/components/layout/Header.tsx` | - | 로고, 네비게이션, 우측 액션 |
| **SearchBar** | `src/components/layout/SearchBar.tsx` | - | 검색 입력창 |
| **Footer** | `src/components/layout/Footer.tsx` | - | 사이트 하단 정보 |
| **HeroBanner** | `src/components/sections/HeroBanner.tsx` | - | 프로모션 배너 |
| **RecommendedCourses** | `src/components/sections/RecommendedCourses.tsx` | `courses: Course[]` | 추천강의 섹션 |
| **RecommendedBooks** | `src/components/sections/RecommendedBooks.tsx` | `books: Book[]` | 추천도서 섹션 |
| **CourseCard** | `src/components/cards/CourseCard.tsx` | `course: Course` | 개별 강의 카드 |
| **BookCard** | `src/components/cards/BookCard.tsx` | `book: Book` | 개별 도서 카드 |

### 5.2 Props Interface

```typescript
// Header - props 없음 (내부 상수 사용)
// 메뉴 데이터:
const menuItems: MenuItem[] = [
  { label: "강의", href: "/courses" },
  { label: "도서", href: "/books" },
  { label: "전문가상담", href: "/consulting" },
  { label: "커뮤니티", href: "/community" },
];

// RecommendedCourses
interface RecommendedCoursesProps {
  courses: Course[];
}

// RecommendedBooks
interface RecommendedBooksProps {
  books: Book[];
}

// CourseCard
interface CourseCardProps {
  course: Course;
}

// BookCard
interface BookCardProps {
  book: Book;
}
```

---

## 6. Responsive Breakpoints

| Breakpoint | Width | Grid Columns | Layout Changes |
|------------|-------|:------------:|----------------|
| **Mobile** | < 640px | 1 | 햄버거 메뉴, 세로 스택, 검색바 축소 |
| **Tablet** | 640px ~ 1023px | 2 | 네비 표시, 2열 그리드 |
| **Desktop** | >= 1024px | 4 | 풀 헤더, 4열 그리드, max-w-1200px |

```
Tailwind 클래스 패턴:
grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6
```

---

## 7. Tailwind Config 업데이트

### 7.1 tailwind.config.js 최종 설계

```javascript
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{js,ts,jsx,tsx}"],
  theme: {
    extend: {
      colors: {
        blue: {
          primary: "#155dfc",
          deep: "#1447e6",
          medium: "#2b7fff",
          light: "#50a2ff",
          tint: "#eff6ff",
          gray: "#abb2bf",
        },
        dark: {
          DEFAULT: "#282c34",
          navy: "#101828",
          deep: "#1e2839",
          charcoal: "#282c34",
          gray: "#4a5565",
          muted: "#6a7282",
          black: "#000000",
        },
      },
      fontFamily: {
        logo: ['"Squada One"', 'cursive'],
        sans: ['"Noto Sans KR"', 'sans-serif'],
      },
      maxWidth: {
        container: "1200px",
      },
    },
  },
  plugins: [],
};
```

### 7.2 Google Fonts 설정 (layout.tsx)

```typescript
import { Squada_One, Noto_Sans_KR } from "next/font/google";

const squadaOne = Squada_One({
  weight: "400",
  subsets: ["latin"],
  variable: "--font-squada-one",
  display: "swap",
});

const notoSansKR = Noto_Sans_KR({
  subsets: ["latin"],
  variable: "--font-noto-sans-kr",
  display: "swap",
});
```

---

## 8. Sample Data 설계

### 8.1 courses.ts (8개 샘플)

```typescript
export const sampleCourses: Course[] = [
  {
    id: "c1",
    title: "비즈니스 데이터 분석 입문 - 엑셀부터 파이썬까지",
    instructor: "김데이터",
    thumbnail: "/images/courses/course-1.jpg",
    price: 55000,
    originalPrice: 77000,
    discountRate: 30,
    rating: 4.8,
    reviewCount: 234,
    studentCount: 1200,
    badges: [{ label: "베스트", variant: "primary" }],
  },
  // ... 7개 더
];
```

### 8.2 books.ts (8개 샘플)

```typescript
export const sampleBooks: Book[] = [
  {
    id: "b1",
    title: "린 스타트업 - 지속적 혁신을 실현하는 창업의 과학",
    author: "에릭 리스",
    cover: "/images/books/book-1.jpg",
    price: 22500,
    originalPrice: 25000,
    discountRate: 10,
    rating: 4.7,
    reviewCount: 89,
    badges: [{ label: "베스트셀러", variant: "green" }],
  },
  // ... 7개 더
];
```

---

## 9. File Structure (최종)

```
project_002/
├── tailwind.config.js          # 색상/폰트 토큰
├── package.json
├── next.config.js
├── tsconfig.json
├── postcss.config.js
├── public/
│   └── images/
│       ├── courses/            # 강의 썸네일 (placeholder)
│       │   ├── course-1.jpg
│       │   └── ...
│       └── books/              # 도서 표지 (placeholder)
│           ├── book-1.jpg
│           └── ...
├── src/
│   ├── app/
│   │   ├── layout.tsx          # Root layout (fonts, metadata, Header/Footer)
│   │   ├── page.tsx            # Main page (Hero, Courses, Books)
│   │   └── globals.css         # @tailwind directives + custom
│   ├── components/
│   │   ├── layout/
│   │   │   ├── Header.tsx      # FR-01, FR-02, FR-03
│   │   │   ├── SearchBar.tsx   # FR-04
│   │   │   └── Footer.tsx      # FR-10
│   │   ├── sections/
│   │   │   ├── HeroBanner.tsx           # FR-05
│   │   │   ├── RecommendedCourses.tsx   # FR-06
│   │   │   └── RecommendedBooks.tsx     # FR-08
│   │   └── cards/
│   │       ├── CourseCard.tsx  # FR-07
│   │       └── BookCard.tsx    # FR-09
│   ├── data/
│   │   ├── courses.ts          # 샘플 강의 데이터
│   │   └── books.ts            # 샘플 도서 데이터
│   └── types/
│       └── index.ts            # Course, Book, Badge, MenuItem
└── docs/
    ├── 01-plan/features/
    │   └── bizschool-main.plan.md
    └── 02-design/features/
        └── bizschool-main.design.md  ← (이 문서)
```

---

## 10. Implementation Order

### 10.1 구현 순서 (의존성 기반)

```
Phase 1: 프로젝트 초기화
  1. [ ] Next.js 프로젝트 생성 (npx create-next-app@latest)
  2. [ ] tailwind.config.js 업데이트 (색상 + 폰트)
  3. [ ] Google Fonts 설정 (layout.tsx)
  4. [ ] globals.css 설정

Phase 2: 타입 & 데이터
  5. [ ] types/index.ts 작성 (Course, Book, Badge, MenuItem)
  6. [ ] data/courses.ts 작성 (샘플 8개)
  7. [ ] data/books.ts 작성 (샘플 8개)

Phase 3: 레이아웃 컴포넌트
  8. [ ] Header.tsx 구현 (로고 + 네비 + 우측 아이콘)
  9. [ ] SearchBar.tsx 구현
  10. [ ] Footer.tsx 구현
  11. [ ] layout.tsx 조합 (Header + SearchBar + Footer)

Phase 4: 카드 컴포넌트
  12. [ ] CourseCard.tsx 구현
  13. [ ] BookCard.tsx 구현

Phase 5: 섹션 & 페이지 조합
  14. [ ] HeroBanner.tsx 구현
  15. [ ] RecommendedCourses.tsx 구현
  16. [ ] RecommendedBooks.tsx 구현
  17. [ ] page.tsx 조합 (모든 섹션 배치)

Phase 6: 반응형 & 마무리
  18. [ ] 모바일 햄버거 메뉴 구현
  19. [ ] 반응형 레이아웃 테스트 및 조정
  20. [ ] placeholder 이미지 준비
```

### 10.2 FR 매핑

| 구현 단계 | 관련 FR | 파일 |
|-----------|---------|------|
| 8 | FR-01, FR-02, FR-03 | Header.tsx |
| 9 | FR-04 | SearchBar.tsx |
| 10 | FR-10 | Footer.tsx |
| 12 | FR-07 | CourseCard.tsx |
| 13 | FR-09 | BookCard.tsx |
| 14 | FR-05 | HeroBanner.tsx |
| 15 | FR-06 | RecommendedCourses.tsx |
| 16 | FR-08 | RecommendedBooks.tsx |
| 18-19 | FR-11 | 전체 컴포넌트 |

---

## 11. Coding Convention

### 11.1 This Feature's Conventions

| Item | Convention |
|------|-----------|
| Component naming | PascalCase (Header.tsx, CourseCard.tsx) |
| File organization | components/{category}/{Component}.tsx |
| State management | N/A (정적 데이터, props drilling) |
| Styling | Tailwind 유틸리티 클래스, 커스텀 토큰 사용 |
| Font usage | 로고: `font-logo`, 전체 UI: `font-sans` |
| Color usage | 직접 hex 사용 금지 → Tailwind 토큰만 사용 |
| Image | next/image 사용, width/height 필수 명시 |

### 11.2 Import Order

```typescript
// 1. React / Next.js
import Image from "next/image";
import Link from "next/link";

// 2. Third-party
import { Search, User, GraduationCap } from "lucide-react";

// 3. Local components
import { CourseCard } from "@/components/cards/CourseCard";

// 4. Types
import type { Course } from "@/types";

// 5. Data
import { sampleCourses } from "@/data/courses";
```

---

## Version History

| Version | Date | Changes | Author |
|---------|------|---------|--------|
| 0.1 | 2026-02-25 | Initial draft - 인프런 벤치마킹 기반 상세 설계 | Allen |
