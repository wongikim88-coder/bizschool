# Design: 공개교육 페이지 (public-education)

> Plan 문서: `docs/01-plan/features/public-education.plan.md`

## 1. 파일 구조

```
src/
├── app/
│   └── education/
│       └── page.tsx              # 공개교육 메인 페이지 (Server Component)
├── components/
│   └── education/
│       ├── MonthSelector.tsx      # 연도/월 선택 필터 (Client Component)
│       ├── CourseSearch.tsx        # 교육과정 검색 입력 (Client Component)
│       └── CourseTable.tsx         # 카테고리별 교육과정 테이블 (Server Component)
├── data/
│   └── education.ts               # Mock 교육과정 데이터 + 타입 정의
└── types/
    └── index.ts                   # EducationCourse 타입 추가
```

## 2. 타입 정의

`src/types/index.ts`에 추가:

```typescript
// ── 공개교육 ──

export interface EducationCourse {
  id: number;
  category: string;          // "세무회계" | "특강" | "재산제세(이론)" 등
  title: string;
  dateRange: string;         // "2026-04-03 ~ 2026-04-11(매주 금토)"
  timeRange: string;         // "13:00 ~ 19:00 (4일 24시간)"
  fee: number;               // 550000
  instructor: string;        // "고경희 세무사"
  status: "open" | "closed";
}
```

## 3. 데이터 파일

`src/data/education.ts`:

```typescript
import type { EducationCourse } from "@/types";

// 카테고리 색상 매핑
export const categoryColors: Record<string, string> = {
  "세무회계": "bg-blue-600",
  "특강": "bg-amber-500",
  "재산제세(이론)": "bg-emerald-600",
};

// 연도 범위
export const YEAR_RANGE = { min: 2006, max: 2027 };

// 2026년 4월 교육과정 데이터 (참조 사이트 기반)
export const educationCourses: EducationCourse[] = [
  {
    id: 1,
    category: "세무회계",
    title: "고경희 세무사의 상속증여세 실무(2026)",
    dateRange: "2026-04-03 ~ 2026-04-11(매주 금토)",
    timeRange: "13:00 ~ 19:00 (4일 24시간)",
    fee: 550000,
    instructor: "고경희 세무사",
    status: "open",
  },
  {
    id: 2,
    category: "특강",
    title: "MSO법인과 병의원 절세컨설팅",
    dateRange: "2026-04-08(수)",
    timeRange: "13:00 ~ 17:00 (1일 4시간)",
    fee: 200000,
    instructor: "윤창인 공인회계사, 세무사",
    status: "open",
  },
  {
    id: 3,
    category: "재산제세(이론)",
    title: "주택임대사업자의 모든 것(2026)",
    dateRange: "2026-04-15(수)",
    timeRange: "13:00 ~ 18:00 (1일 5시간)",
    fee: 150000,
    instructor: "지병근 세무사",
    status: "open",
  },
  {
    id: 4,
    category: "특강",
    title: "법인컨설팅을 위한 보험세무 핵심포인트(2026)",
    dateRange: "2026-04-16(목)",
    timeRange: "13:00 ~ 18:00 (1일 5시간)",
    fee: 150000,
    instructor: "박진호 세무사",
    status: "open",
  },
  {
    id: 5,
    category: "재산제세(이론)",
    title: "정문현교수의 핵심실무 양도소득세(2026)",
    dateRange: "2026-04-17 ~ 2026-04-25(금토)",
    timeRange: "13:00 ~ 19:00 (3일 18시간)",
    fee: 420000,
    instructor: "정문현 교수",
    status: "open",
  },
  {
    id: 6,
    category: "재산제세(이론)",
    title: "2026부동산세금의 정석(Ⅰ)",
    dateRange: "2026-04-22(수)",
    timeRange: "13:00 ~ 18:00 (1일 5시간)",
    fee: 150000,
    instructor: "지병근 세무사",
    status: "open",
  },
  {
    id: 7,
    category: "특강",
    title: "국세청 세무조사, 금융조사 및 조세범칙조사",
    dateRange: "2026-04-23(목)",
    timeRange: "13:00 ~ 17:00 (1일 4시간)",
    fee: 220000,
    instructor: "윤창인 공인회계사, 세무사",
    status: "open",
  },
  {
    id: 8,
    category: "재산제세(이론)",
    title: "2026부동산세금의 정석(Ⅱ)",
    dateRange: "2026-04-29(수)",
    timeRange: "13:00 ~ 18:00 (1일 5시간)",
    fee: 150000,
    instructor: "지병근 세무사",
    status: "open",
  },
];

// 과정을 카테고리별로 그룹화 (원본 순서 유지)
export function groupByCategory(courses: EducationCourse[]) {
  const groups: { category: string; courses: EducationCourse[] }[] = [];
  let currentCategory = "";

  for (const course of courses) {
    if (course.category !== currentCategory) {
      groups.push({ category: course.category, courses: [course] });
      currentCategory = course.category;
    } else {
      groups[groups.length - 1].courses.push(course);
    }
  }

  return groups;
}

// 금액 포맷
export function formatFee(fee: number): string {
  return fee.toLocaleString("ko-KR") + "원";
}
```

## 4. 컴포넌트 상세 설계

### 4.1 페이지: `src/app/education/page.tsx`

**타입**: Server Component (searchParams 사용)

```
┌─────────────────────────────────────────────────────────┐
│  Hero Section (공개교육)                                  │
│  bg-gradient-to-br from-[#155dfc] to-[#0d3b9e]          │
├─────────────────────────────────────────────────────────┤
│  max-w-[1200px] mx-auto px-4                            │
│  ┌───────────────────────────────────────────────────┐  │
│  │  <CourseSearch />                                  │  │
│  ├───────────────────────────────────────────────────┤  │
│  │  <MonthSelector year={} month={} />               │  │
│  ├───────────────────────────────────────────────────┤  │
│  │  <CourseTable courses={filteredCourses} />         │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

**Props / searchParams**:
```typescript
interface EducationPageProps {
  searchParams: Promise<{
    year?: string;
    month?: string;
    search?: string;
  }>;
}
```

**로직**:
1. searchParams에서 year, month 추출 (기본값: 현재 연도, 현재 월)
2. educationCourses에서 해당 year/month 데이터 필터링
3. search가 있으면 과정명 기준 추가 필터링
4. 필터된 데이터를 CourseTable에 전달

**metadata**:
```typescript
export const metadata: Metadata = {
  title: "공개교육 | BIZSCHOOL",
  description: "월별 공개교육 일정을 확인하고 수강신청하세요. 세무회계, 재산제세, 특강 등 전문가 교육과정을 제공합니다.",
};
```

### 4.2 컴포넌트: `MonthSelector.tsx`

**타입**: Client Component (`"use client"`)

```
┌──────────────────────────────────────────────────────────┐
│  [2026 ▼]  해당교육과정을 클릭하시면 교육내용 및          │
│            수강신청을 하실 수 있습니다.                     │
│                                                           │
│  ┌────┬────┬────┬════┬────┬────┬────┬────┬────┬────┬────┬────┐  │
│  │1월 │2월 │3월 │4월 │5월 │6월 │7월 │8월 │9월 │10월│11월│12월│  │
│  └────┴────┴────┴════┴────┴────┴────┴────┴────┴────┴────┴────┘  │
│                   ▲ 선택된 월: primary 색상 + 하단 보더           │
└──────────────────────────────────────────────────────────┘
```

**Props**:
```typescript
interface MonthSelectorProps {
  currentYear: number;
  currentMonth: number;
}
```

**동작**:
- 연도 변경: `<select>` 드롭다운으로 year 변경 → URL 업데이트
- 월 변경: 월 탭 클릭 → URL 업데이트
- `useRouter` + `useSearchParams` 사용 (books/CategoryFilter.tsx 패턴 참조)
- 선택된 월: `border-b-2 border-[var(--color-primary)] font-semibold text-[var(--color-dark)]`
- 비선택 월: `text-[var(--color-muted)] hover:text-[var(--color-dark)]`

### 4.3 컴포넌트: `CourseSearch.tsx`

**타입**: Client Component (`"use client"`)

```
┌─────────────────────────────────────────┐
│  🔍  검색어를 입력하세요              │
└─────────────────────────────────────────┘
```

**Props**:
```typescript
interface CourseSearchProps {
  defaultValue?: string;
}
```

**동작**:
- 검색어 입력 후 Enter 또는 검색 버튼 클릭 시 URL에 `search` 파라미터 추가
- 기존 BookSearch 컴포넌트 패턴 참조
- `rounded-xl border border-[var(--color-border)]` 스타일

### 4.4 컴포넌트: `CourseTable.tsx`

**타입**: Server Component

```
┌──────────────────────────────────────────────────────────┐
│  ▶ 세무회계                                              │
│  ┌──────────┬──────────┬──────────┬──────┬──────┬──────┐ │
│  │교육과정명 │ 교육일시  │ 교육시간  │교육비│ 교수  │ 접수 │ │
│  ├──────────┼──────────┼──────────┼──────┼──────┼──────┤ │
│  │고경희 ... │04-03~... │13:00~... │550..│고경희│수강신청│ │
│  └──────────┴──────────┴──────────┴──────┴──────┴──────┘ │
│                                                           │
│  ▶ 특강                                                   │
│  ┌──────────┬──────────┬──────────┬──────┬──────┬──────┐ │
│  │교육과정명 │ 교육일시  │ 교육시간  │교육비│ 교수  │ 접수 │ │
│  ├──────────┼──────────┼──────────┼──────┼──────┼──────┤ │
│  │MSO법인...│04-08(수) │13:00~... │200..│윤창인│수강신청│ │
│  └──────────┴──────────┴──────────┴──────┴──────┴──────┘ │
└──────────────────────────────────────────────────────────┘

[Mobile: 카드 레이아웃으로 전환]
┌───────────────────────────────┐
│  세무회계                      │
│  ┌───────────────────────────┐│
│  │ 고경희 세무사의 상속증여세 ││
│  │ 실무(2026)                 ││
│  │                            ││
│  │ 📅 04-03 ~ 04-11(금토)    ││
│  │ 🕐 13:00~19:00 (4일 24h)  ││
│  │ 💰 550,000원               ││
│  │ 👤 고경희 세무사            ││
│  │                            ││
│  │ [수강신청]                  ││
│  └───────────────────────────┘│
└───────────────────────────────┘
```

**Props**:
```typescript
interface CourseTableProps {
  courses: EducationCourse[];
}
```

**구현 상세**:
- `groupByCategory()` 함수로 카테고리별 그룹화
- 각 그룹마다: 카테고리 헤더 + 테이블 헤더 + 과정 행
- 카테고리 헤더: 색상 dot + 카테고리명 (좌측 정렬, bold)
- 테이블 헤더: 회색 배경 (`bg-[var(--color-light-bg)]`)
- 과정 행: hover 시 배경색 변경 (`hover:bg-[var(--color-light-bg)]`)
- 수강신청 버튼:
  - open: `bg-[var(--color-primary)] text-white rounded-lg px-4 py-1.5 text-sm`
  - closed: `bg-gray-200 text-gray-400 cursor-not-allowed`

**반응형**:
- Desktop (md+): `<table>` 형태 그대로 표시
- Mobile (<md): `hidden md:table` + 카드 레이아웃 `md:hidden`
- 카드 레이아웃: 각 과정을 border-rounded 카드로 표시, 수직 배열

**빈 상태**:
- 해당 월에 과정이 없을 때: "해당 월에 예정된 교육과정이 없습니다" 메시지

## 5. Header 네비게이션 변경

`src/components/layout/Header.tsx`:

```typescript
// Before
const menuItems: MenuItem[] = [
  { label: "강의", href: "/" },
  { label: "도서", href: "/books" },
  { label: "전문가상담", href: "/consulting" },
  { label: "커뮤니티", href: "/community" },
];

// After
const menuItems: MenuItem[] = [
  { label: "공개교육", href: "/education" },
  { label: "강의", href: "/" },
  { label: "도서", href: "/books" },
  { label: "전문가상담", href: "/consulting" },
  { label: "커뮤니티", href: "/community" },
];
```

## 6. 구현 순서

| 순서 | 작업 | 파일 | 의존성 |
|------|------|------|--------|
| 1 | 타입 정의 추가 | `src/types/index.ts` | 없음 |
| 2 | 데이터 파일 생성 | `src/data/education.ts` | 타입 |
| 3 | MonthSelector 컴포넌트 | `src/components/education/MonthSelector.tsx` | 없음 |
| 4 | CourseSearch 컴포넌트 | `src/components/education/CourseSearch.tsx` | 없음 |
| 5 | CourseTable 컴포넌트 | `src/components/education/CourseTable.tsx` | 타입, 데이터 |
| 6 | 페이지 조립 | `src/app/education/page.tsx` | 모든 컴포넌트 |
| 7 | Header 메뉴 추가 | `src/components/layout/Header.tsx` | 없음 |

## 7. 디자인 토큰 (기존 시스템 활용)

| 용도 | CSS 변수 | 값 |
|------|---------|-----|
| 히어로 배경 | gradient `#155dfc` → `#0d3b9e` | 기존 about/directions 동일 |
| 텍스트 (제목) | `--color-dark` | `#282c34` |
| 텍스트 (본문) | `--color-body` | `#4a5565` |
| 텍스트 (부가) | `--color-muted` | `#6a7282` |
| 테두리 | `--color-border` | `#e5e7eb` |
| 배경 (밝은) | `--color-light-bg` | `#f8f9fa` |
| 액센트 (버튼) | `--color-primary` | `#155dfc` |
| 카테고리: 세무회계 | `bg-blue-600` | Tailwind blue |
| 카테고리: 특강 | `bg-amber-500` | Tailwind amber |
| 카테고리: 재산제세 | `bg-emerald-600` | Tailwind emerald |

## 8. 반응형 브레이크포인트

| 화면 | 너비 | 레이아웃 |
|------|------|---------|
| Mobile | < 768px | 카드 레이아웃, 검색/필터 세로 배치 |
| Desktop | >= 768px | 테이블 레이아웃, 검색/필터 가로 배치 |

## 9. 접근성

- 월 선택 탭: `role="tablist"`, `role="tab"`, `aria-selected`
- 연도 선택: `<select>` 네이티브 요소 (자체 접근성)
- 검색 필드: `aria-label="교육과정 검색"`
- 수강신청 버튼 (마감): `aria-disabled="true"`
- 테이블: semantic `<table>`, `<thead>`, `<tbody>`, `<th>`, `<td>`
