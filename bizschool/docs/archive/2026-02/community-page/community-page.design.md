# Design: 커뮤니티 페이지 (community-page)

> Feature: 커뮤니티 페이지 구현 (홈/강의질문/상담사례/소통공간)
> Created: 2026-02-27
> Status: Design
> Plan Reference: `docs/01-plan/features/community-page.plan.md`

---

## 1. 아키텍처 개요

```
/community (page.tsx - Server Component)
├── CommunityTabs          - 탭 네비게이션 (Client)
├── [tab === "home"]
│   ├── HomeTab            - 홈: 인기 게시물 모아보기 (Server)
│   │   ├── PostCard (x N) - 섹션별 인기 게시물
│   │   └── WeeklyTopUsers - 우측 패널: 주간 활동 TOP 10 (Server)
├── [tab === "questions"]
│   ├── QuestionsTab       - 강의질문 목록 (Server)
│   │   ├── PostCard (x N)
│   │   └── Pagination     - 페이지네이션 (Client)
├── [tab === "cases"]
│   ├── CasesTab           - 상담사례 목록 (Server)
│   │   ├── PostCard (x N)
│   │   └── Pagination
├── [tab === "discussion"]
│   ├── DiscussionTab      - 소통공간 목록 (Server)
│   │   ├── PostCard (x N)
│   │   └── Pagination
```

### 1.1 데이터 흐름

```
URL Query Params (?tab=home&page=1)
         ↓
  /community/page.tsx (Server Component)
    - searchParams에서 tab, page 추출
    - tab에 따라 해당 데이터 필터링
    - page에 따라 페이지네이션 처리
         ↓
  CommunityTabs (Client) → URL 변경으로 탭 전환
  탭별 콘텐츠 컴포넌트 (Server) → 데이터 표시
  Pagination (Client) → URL 변경으로 페이지 이동
```

- **Server Component** (`page.tsx`): URL searchParams로 탭/페이지 상태 관리, mock 데이터 필터링
- **Client Components** (`CommunityTabs`, `Pagination`): URL 변경으로 상태 전환 트리거

---

## 2. 데이터 모델 상세

### 2.1 타입 정의

```typescript
// src/types/index.ts 에 추가

// 커뮤니티 게시물 공통 타입
export interface CommunityPost {
  id: string;
  title: string;
  author: string;
  category: string;
  createdAt: string;       // "2026.02.25" 형식
  viewCount: number;
  commentCount: number;
}

// 강의질문 게시물
export interface CourseQuestion extends CommunityPost {
  type: "question";
  courseName: string;       // 관련 강의명
  answerCount: number;      // 답변 수
  isAnswered: boolean;      // 답변 완료 여부
}

// 상담사례 게시물
export interface ConsultationCase extends CommunityPost {
  type: "consultation";
  isVerified: boolean;      // 전문가 검증 완료 여부
  verifiedBy?: string;      // 검증 전문가명
  helpfulCount: number;     // "도움됐어요" 수
}

// 소통공간 게시물
export interface DiscussionPost extends CommunityPost {
  type: "discussion";
  subCategory: "잡담" | "정보공유" | "질문" | "후기";
}

// 주간 활동 유저
export interface WeeklyActiveUser {
  rank: number;
  nickname: string;
  points: number;
  badge?: "gold" | "silver" | "bronze";
}

// 탭 타입
export type CommunityTab = "home" | "questions" | "cases" | "discussion";
```

### 2.2 카테고리 정의

```typescript
// src/data/community.ts

export const communityTabs = [
  { key: "home" as const, label: "홈" },
  { key: "questions" as const, label: "강의질문" },
  { key: "cases" as const, label: "상담사례" },
  { key: "discussion" as const, label: "소통공간" },
] as const;

export const questionCategories = ["재무회계", "세무실무", "경영전략", "인사노무", "ERP실무"] as const;

export const caseCategories = ["급여/4대보험", "퇴직/정산", "연말정산", "부가세", "법인세", "기타세무"] as const;

export const discussionSubCategories = ["잡담", "정보공유", "질문", "후기"] as const;

export const POSTS_PER_PAGE = 10;
export const HOME_POSTS_PER_SECTION = 5;
```

---

## 3. 컴포넌트 상세 설계

### 3.1 `/community/page.tsx` (Server Component)

**역할**: 커뮤니티 메인 페이지 루트. URL searchParams로 탭/페이지 상태 관리.

```typescript
interface CommunityPageProps {
  searchParams: Promise<{
    tab?: string;
    page?: string;
  }>;
}
```

**데이터 처리 로직**:
1. `tab` 파라미터로 현재 탭 결정 (기본: "home")
2. `page` 파라미터로 현재 페이지 결정 (기본: 1, 홈 탭 제외)
3. 탭에 따라 해당 mock 데이터 로드
4. `POSTS_PER_PAGE`(10)으로 페이지네이션

**레이아웃 구조**:
```tsx
<div className="mx-auto max-w-[1200px] px-4 py-6">
  <CommunityTabs currentTab={tab} />
  {tab === "home" && <HomeTab />}
  {tab === "questions" && <QuestionsTab page={page} />}
  {tab === "cases" && <CasesTab page={page} />}
  {tab === "discussion" && <DiscussionTab page={page} />}
</div>
```

---

### 3.2 `CommunityTabs.tsx` (Client Component)

**역할**: 홈/강의질문/상담사례/소통공간 탭 네비게이션.

**Props**:
```typescript
interface CommunityTabsProps {
  currentTab: CommunityTab;
}
```

**동작**:
- 탭 클릭 시 `router.push(/community?tab={key})` (page는 리셋)
- 현재 탭은 하단 border로 강조

**디자인 사양**:
```
┌──────────────────────────────────────────────────────────────┐
│  홈       강의질문       상담사례       소통공간              │
│  ───                                                        │
│  (활성: 하단 2px 파란 border + 파란 텍스트 + bold)           │
└──────────────────────────────────────────────────────────────┘
```

| 요소 | 스타일 |
|------|--------|
| 컨테이너 | `flex border-b border-[var(--color-border)] gap-0` |
| 활성 탭 | `border-b-2 border-[var(--color-primary)] text-[var(--color-primary)] font-bold px-6 py-3` |
| 비활성 탭 | `text-[var(--color-muted)] px-6 py-3 hover:text-[var(--color-body)] transition-colors` |
| 모바일 | `overflow-x-auto whitespace-nowrap` (가로 스크롤) |

---

### 3.3 `PostCard.tsx` (Server Component)

**역할**: 모든 탭에서 사용되는 공통 게시물 카드. 게시물 타입(question/consultation/discussion)에 따라 세부 정보를 다르게 렌더링.

**Props**:
```typescript
interface PostCardProps {
  post: CourseQuestion | ConsultationCase | DiscussionPost;
  showCategoryTag?: boolean;  // 홈에서는 탭 구분 태그 표시
}
```

**레이아웃 (공통)**:
```
┌─────────────────────────────────────────────────────────────────┐
│ [카테고리]  게시물 제목                                  👁 234  │
│ 메타 정보 라인 (작성자 · 날짜 · 답변수/댓글수 · 상태배지)       │
└─────────────────────────────────────────────────────────────────┘
```

**타입별 메타 정보 렌더링**:

| 타입 | 첫째 줄 (좌) | 첫째 줄 (우) | 둘째 줄 |
|------|-------------|-------------|---------|
| `question` | `[카테고리] 제목` | `👁 조회수` | `작성자 · 날짜 · 답변 N` + 상태배지(`답변완료`/`미답변`) |
| `consultation` | `[카테고리] 제목` | `👁 조회수` | `AI답변 · 전문가검증 ✅ · 날짜 · 도움됐어요 N` |
| `discussion` | `[서브카테고리] 제목` | `👁 조회수` | `작성자 · 날짜 · 💬 댓글수` |

**디자인 사양**:

| 요소 | 스타일 |
|------|--------|
| 컨테이너 | `border-b border-[var(--color-border)] px-4 py-4 transition-colors hover:bg-[var(--color-light-bg)] cursor-pointer` |
| 카테고리 태그 | `inline-flex items-center rounded bg-[var(--color-primary)]/10 px-2 py-0.5 text-xs font-medium text-[var(--color-primary)]` |
| 제목 | `text-[var(--color-dark)] font-medium text-[15px] ml-2` |
| 조회수 | `text-[var(--color-muted)] text-sm flex items-center gap-1` |
| 메타 정보 | `text-[var(--color-muted)] text-sm mt-1.5` |
| 답변완료 배지 | `text-emerald-600 text-xs font-medium` |
| 미답변 배지 | `text-[var(--color-red)] text-xs font-medium` |
| 전문가검증 배지 | `text-emerald-600 text-xs font-medium` |
| 홈 탭 구분 태그 | `inline-flex items-center rounded bg-gray-100 px-2 py-0.5 text-xs text-[var(--color-muted)] mr-1` |

---

### 3.4 `HomeTab.tsx` (Server Component)

**역할**: 홈 탭. 강의질문/상담사례/소통공간에서 인기 게시물을 각 5개씩 모아 표시. 우측에 주간 활동 TOP 10 패널.

**레이아웃** (Desktop):
```
┌─────────────────────────────────────────────┬────────────────────────┐
│ 메인 콘텐츠 (flex-1, min-w-0)               │ 우측 패널 (w-[280px])  │
│                                             │                        │
│ ┌─────────────────────────────────────────┐ │ ┌────────────────────┐ │
│ │ 🔥 인기 강의질문         [더보기 >]     │ │ │ 주간 활동 TOP 10   │ │
│ │ ─────────────────────────────────────── │ │ │                    │ │
│ │ PostCard                                │ │ │ 1. 김회계사 520pt  │ │
│ │ PostCard                                │ │ │ 2. 이경리  480pt   │ │
│ │ PostCard                                │ │ │ ...                │ │
│ │ PostCard                                │ │ └────────────────────┘ │
│ │ PostCard                                │ │                        │
│ └─────────────────────────────────────────┘ │                        │
│                                             │                        │
│ ┌─────────────────────────────────────────┐ │                        │
│ │ 📋 인기 상담사례         [더보기 >]     │ │                        │
│ │ ─────────────────────────────────────── │ │                        │
│ │ PostCard x 5                            │ │                        │
│ └─────────────────────────────────────────┘ │                        │
│                                             │                        │
│ ┌─────────────────────────────────────────┐ │                        │
│ │ 💬 인기 소통공간         [더보기 >]     │ │                        │
│ │ ─────────────────────────────────────── │ │                        │
│ │ PostCard x 5                            │ │                        │
│ └─────────────────────────────────────────┘ │                        │
└─────────────────────────────────────────────┴────────────────────────┘
```

**디자인 사양**:

| 요소 | 스타일 |
|------|--------|
| 2열 컨테이너 | `flex gap-6` |
| 좌측 메인 | `flex-1 min-w-0` |
| 우측 패널 | `w-[280px] shrink-0 hidden lg:block` |
| 섹션 헤더 | `flex items-center justify-between py-4` |
| 섹션 제목 | `text-lg font-bold text-[var(--color-dark)]` |
| 더보기 링크 | `text-sm text-[var(--color-muted)] hover:text-[var(--color-primary)] cursor-pointer` |
| 섹션 구분 | `mb-8` (섹션 간 간격) |
| 섹션 카드 컨테이너 | `rounded-lg border border-[var(--color-border)]` |

**"더보기" 동작**: 해당 탭으로 이동 (`?tab=questions`, `?tab=cases`, `?tab=discussion`)

**인기 게시물 정렬**: `viewCount` 기준 내림차순, 상위 5개

---

### 3.5 `WeeklyTopUsers.tsx` (Server Component)

**역할**: 홈 탭 우측 패널. 주간 활동 TOP 10 유저 랭킹.

**레이아웃**:
```
┌────────────────────────────┐
│  🏆 주간 활동 TOP 10       │
│  ─────────────────────     │
│  1  🥇 김회계사    520pt   │
│  2  🥈 이경리      480pt   │
│  3  🥉 박세무      350pt   │
│  4     최재무      290pt   │
│  5     정경영      210pt   │
│  6     한경리      185pt   │
│  7     오회계      170pt   │
│  8     신세무      155pt   │
│  9     강경리      140pt   │
│  10    윤경영      120pt   │
│                            │
│  2026.02.23 기준            │
└────────────────────────────┘
```

**디자인 사양**:

| 요소 | 스타일 |
|------|--------|
| 패널 컨테이너 | `rounded-lg border border-[var(--color-border)] p-5 sticky top-24` |
| 패널 제목 | `text-base font-bold text-[var(--color-dark)] mb-4` |
| 유저 행 | `flex items-center justify-between py-2.5` |
| 순위 번호 | `w-6 text-sm font-bold text-[var(--color-muted)]` |
| 1~3위 순위 | `text-[var(--color-primary)] font-bold` |
| 메달 아이콘 | 1위: `🥇`, 2위: `🥈`, 3위: `🥉` (텍스트 이모지) |
| 닉네임 | `text-sm text-[var(--color-dark)] flex-1 ml-2` |
| 포인트 | `text-xs text-[var(--color-muted)]` |
| 기준 날짜 | `text-xs text-[var(--color-muted)] mt-4 text-center` |
| 구분선 | `divide-y divide-[var(--color-border)]/50` |

**반응형 (태블릿 이하)**:
- `lg:block hidden` → 1024px 미만에서는 숨김
- 태블릿/모바일에서는 메인 콘텐츠 하단에 가로 스크롤 형태로 표시:

```tsx
{/* 모바일/태블릿용 TOP 10 - 메인 콘텐츠 하단 */}
<div className="lg:hidden mt-6">
  <WeeklyTopUsers layout="horizontal" />
</div>
```

모바일 가로 레이아웃:
```
┌────────────────────────────────────────────────────────────────────┐
│ 🏆 주간 활동 TOP 10                                               │
│ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐              │
│ │ 1🥇      │ │ 2🥈      │ │ 3🥉      │ │ 4        │ ← 스크롤 → │
│ │ 김회계사 │ │ 이경리   │ │ 박세무   │ │ 최재무   │              │
│ │ 520pt    │ │ 480pt    │ │ 350pt    │ │ 290pt    │              │
│ └──────────┘ └──────────┘ └──────────┘ └──────────┘              │
└────────────────────────────────────────────────────────────────────┘
```

Props에 `layout` 추가:
```typescript
interface WeeklyTopUsersProps {
  layout?: "vertical" | "horizontal";  // 기본: "vertical"
}
```

---

### 3.6 `QuestionsTab.tsx` (Server Component)

**역할**: 강의질문 탭. 전체 강의질문 게시물을 리스트 형태로 표시.

**Props**:
```typescript
interface QuestionsTabProps {
  page: number;
}
```

**레이아웃**:
```
┌─────────────────────────────────────────────────────────────────────┐
│ 총 N개의 강의질문                                                    │
├─────────────────────────────────────────────────────────────────────┤
│ PostCard (CourseQuestion)                                           │
│ PostCard (CourseQuestion)                                           │
│ PostCard (CourseQuestion)                                           │
│ ...                                                                 │
│ PostCard (CourseQuestion)                                           │
├─────────────────────────────────────────────────────────────────────┤
│                        [ < 1 2 3 4 5 > ]                            │
└─────────────────────────────────────────────────────────────────────┘
```

**디자인 사양**:

| 요소 | 스타일 |
|------|--------|
| 컨테이너 | 없음 (page.tsx 내부 직접 배치) |
| 게시물 수 텍스트 | `text-sm text-[var(--color-muted)] mb-2 py-3` |
| 게시물 리스트 | `rounded-lg border border-[var(--color-border)] divide-y divide-[var(--color-border)]` |
| 페이지네이션 | 기존 books/Pagination 로직 재사용 (basePath 파라미터 추가) |

---

### 3.7 `CasesTab.tsx` (Server Component)

**역할**: 상담사례 탭. 전문가 검증 완료된 AI 상담 사례 리스트.

**Props**:
```typescript
interface CasesTabProps {
  page: number;
}
```

QuestionsTab과 동일한 레이아웃 구조. PostCard에 `ConsultationCase` 타입 전달.

---

### 3.8 `DiscussionTab.tsx` (Server Component)

**역할**: 소통공간 탭. 자유 게시글 리스트 + 글쓰기 버튼.

**Props**:
```typescript
interface DiscussionTabProps {
  page: number;
}
```

**레이아웃**:
```
┌─────────────────────────────────────────────────────────────────────┐
│ 총 N개의 게시글                                    [✏️ 글쓰기]      │
├─────────────────────────────────────────────────────────────────────┤
│ PostCard (DiscussionPost)                                          │
│ PostCard (DiscussionPost)                                          │
│ ...                                                                │
├─────────────────────────────────────────────────────────────────────┤
│                        [ < 1 2 3 4 5 > ]                           │
└─────────────────────────────────────────────────────────────────────┘
```

**글쓰기 버튼** (UI만):

| 요소 | 스타일 |
|------|--------|
| 버튼 | `bg-[var(--color-primary)] text-white rounded-lg px-4 py-2 text-sm font-medium flex items-center gap-1.5` |
| 아이콘 | `Pencil` (lucide-react, size=14) |
| hover | `hover:bg-[var(--color-primary-dark)] transition-colors` |

---

### 3.9 `Pagination.tsx` (Client Component) - 커뮤니티용

**역할**: 커뮤니티 페이지용 페이지네이션. 기존 books/Pagination과 로직 동일, `basePath`를 props로 받아 범용화.

**Props**:
```typescript
interface CommunityPaginationProps {
  currentPage: number;
  totalPages: number;
  currentTab: CommunityTab;
}
```

**동작**:
- 페이지 클릭 시 `router.push(/community?tab={currentTab}&page={n})`
- 기존 Pagination과 동일한 로직 (최대 5개 번호, 이전/다음 버튼)
- **디자인**: 기존 books/Pagination과 완전 동일한 스타일 유지

---

## 4. Mock 데이터 설계

### 4.1 `src/data/community.ts`

**데이터 분포**:

| 데이터 | 건수 | 정렬 기준 |
|--------|------|----------|
| 강의질문 (CourseQuestion) | 15개 | 최신순 (createdAt) |
| 상담사례 (ConsultationCase) | 15개 | 최신순 |
| 소통공간 (DiscussionPost) | 15개 | 최신순 |
| 주간 활동 유저 (WeeklyActiveUser) | 10명 | 포인트순 (rank) |

**강의질문 데이터 상세**:
- 카테고리 분포: 재무회계 4, 세무실무 3, 경영전략 3, 인사노무 3, ERP실무 2
- 답변완료: 10개 (67%), 미답변: 5개 (33%)
- 조회수 범위: 50~800
- 날짜 범위: 2026.02.10 ~ 2026.02.27

**상담사례 데이터 상세**:
- 카테고리 분포: 급여/4대보험 3, 퇴직/정산 3, 연말정산 3, 부가세 2, 법인세 2, 기타세무 2
- 전부 검증 완료 (`isVerified: true`)
- 도움됐어요 범위: 5~50
- 조회수 범위: 100~1200

**소통공간 데이터 상세**:
- 서브카테고리 분포: 잡담 5, 정보공유 4, 질문 3, 후기 3
- 댓글수 범위: 2~60
- 조회수 범위: 80~2000

**주간 활동 유저 데이터**:
- 1~3위: gold/silver/bronze 배지
- 포인트 범위: 120~520

---

## 5. URL 라우팅 설계

### 5.1 URL 파라미터

| 파라미터 | 타입 | 기본값 | 예시 |
|----------|------|--------|------|
| `tab` | CommunityTab | "home" | `?tab=questions` |
| `page` | number | 1 | `?tab=questions&page=2` |

### 5.2 URL 예시

```
/community                          → 홈 탭
/community?tab=home                 → 홈 탭 (명시적)
/community?tab=questions            → 강의질문, 1페이지
/community?tab=questions&page=2     → 강의질문, 2페이지
/community?tab=cases                → 상담사례, 1페이지
/community?tab=discussion           → 소통공간, 1페이지
/community?tab=discussion&page=2    → 소통공간, 2페이지
```

### 5.3 파라미터 변경 시 동작

| 액션 | tab | page |
|------|-----|------|
| 탭 변경 | 변경됨 | 1로 리셋 |
| 페이지 이동 | 유지 | 변경됨 |
| 홈 탭 더보기 클릭 | 해당 탭으로 변경 | 1로 리셋 |

---

## 6. 구현 순서 (Implementation Order)

| # | 파일 | 컴포넌트/역할 | 의존성 |
|---|------|-------------|--------|
| 1 | `src/types/index.ts` | 커뮤니티 타입 추가 | 없음 |
| 2 | `src/data/community.ts` | Mock 데이터 + 카테고리 상수 + POSTS_PER_PAGE | #1 |
| 3 | `src/components/community/PostCard.tsx` | 공통 게시물 카드 | #1 |
| 4 | `src/components/community/WeeklyTopUsers.tsx` | 주간 활동 TOP 10 패널 | #1, #2 |
| 5 | `src/components/community/CommunityTabs.tsx` | 탭 네비게이션 | #1 |
| 6 | `src/components/community/CommunityPagination.tsx` | 페이지네이션 (커뮤니티용) | #1 |
| 7 | `src/components/community/HomeTab.tsx` | 홈 탭 | #2, #3, #4 |
| 8 | `src/components/community/QuestionsTab.tsx` | 강의질문 탭 | #2, #3, #6 |
| 9 | `src/components/community/CasesTab.tsx` | 상담사례 탭 | #2, #3, #6 |
| 10 | `src/components/community/DiscussionTab.tsx` | 소통공간 탭 | #2, #3, #6 |
| 11 | `src/app/community/page.tsx` | 커뮤니티 페이지 조립 | #5, #7~#10 |

---

## 7. 디자인 토큰 매핑

기존 `globals.css`의 CSS 변수 활용 (새 토큰 추가 불필요):

| 용도 | CSS Variable | 값 |
|------|--------------|------|
| 활성 탭, 카테고리 태그 | `--color-primary` | `#155dfc` |
| 활성 탭 hover | `--color-primary-dark` | `#1447e6` |
| 카테고리 태그 배경 | `--color-primary` + opacity 10% | `#155dfc/10` |
| 제목 텍스트 | `--color-dark` | `#282c34` |
| 본문 텍스트 | `--color-body` | `#4a5565` |
| 보조 텍스트 (날짜, 조회수) | `--color-muted` | `#6a7282` |
| 테두리 | `--color-border` | `#e5e7eb` |
| hover 배경, 설명박스 | `--color-light-bg` | `#f8f9fa` |
| 미답변 상태 | `--color-red` | `#fa5252` |
| 답변완료/검증완료 | Tailwind `emerald-600` | `#059669` |
| 글쓰기 버튼 hover | `--color-primary-dark` | `#1447e6` |

**Tailwind CSS v4 주의**: 반드시 `var()` 래퍼 사용
- `bg-[var(--color-primary)]` (O)
- `bg-[--color-primary]` (X - 투명하게 렌더링됨)

---

## 8. 반응형 브레이크포인트

| 브레이크포인트 | 변경 사항 |
|----------------|----------|
| `< 640px` (모바일) | 탭 가로 스크롤, PostCard 조회수 숨김, TOP 10 가로 스크롤 |
| `640px ~ 1023px` (태블릿) | 1열 레이아웃, TOP 10 하단 배치 |
| `>= 1024px` (데스크톱) | 2열 레이아웃 (홈), TOP 10 우측 사이드바 |

### 상세 반응형 처리

**탭 네비게이션**:
- 모바일: `overflow-x-auto whitespace-nowrap`, 스크롤바 숨김
- 데스크톱: 자연 가로 배치

**PostCard**:
- 모바일: 조회수 아이콘 숨김 (`hidden sm:flex`)
- 데스크톱: 전체 정보 표시

**HomeTab**:
- 모바일/태블릿: 1열, TOP 10 하단 가로 스크롤
- 데스크톱: 2열 (메인 + 사이드바)

---

## 9. 접근성 요구사항

| 컴포넌트 | 접근성 처리 |
|----------|-------------|
| CommunityTabs | `role="tablist"`, 각 탭 `role="tab"`, `aria-selected`, `aria-controls` |
| PostCard | `article` 태그, 제목 `h3`, 카테고리 `aria-label` |
| WeeklyTopUsers | `aside` 태그, `aria-label="주간 활동 TOP 10"` |
| Pagination | `nav aria-label="페이지 이동"`, 현재 페이지 `aria-current="page"` |
| 답변상태 배지 | `aria-label="답변완료"` / `aria-label="미답변"` |
| 글쓰기 버튼 | `aria-label="새 글 작성"` |

---

## Version History

| Version | Date | Changes | Author |
|---------|------|---------|--------|
| 0.1 | 2026-02-27 | Initial draft | Claude |
