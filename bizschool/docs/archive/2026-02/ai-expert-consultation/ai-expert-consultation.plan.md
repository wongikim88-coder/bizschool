# AI 전문가 상담 (ai-expert-consultation) Planning Document

> **Summary**: 세무/회계/경리 분야 AI 전문가 상담 페이지 - Gemini 스타일 채팅 UI + 전문가 검증 시스템
>
> **Project**: BIZSCHOOL
> **Version**: 0.1.0
> **Author**: Allen
> **Date**: 2026-02-26
> **Status**: Draft

---

## 1. Overview

### 1.1 Purpose

사용자가 세무, 회계, 경리 관련 질문을 AI에게 직접 상담할 수 있는 채팅 기반 페이지를 제공한다. LLM이 즉각적인 답변을 제공하며, 필요 시 실제 전문가의 검증을 신청하여 답변의 신뢰성을 보장받을 수 있다.

### 1.2 Background

- BIZSCHOOL 사용자들은 세무/회계/경리 관련 즉각적인 답변이 필요하지만, 전문가 상담은 비용과 시간이 소요됨
- AI 상담으로 초기 답변을 빠르게 제공하고, 중요한 의사결정이 필요한 경우 전문가 검증으로 신뢰도를 확보
- Google Gemini 스타일의 모던한 채팅 UI로 직관적인 사용자 경험 제공

### 1.3 Related Documents

- UI 참고: [Google Gemini](https://gemini.google.com/)
- 아카이브: `docs/archive/2026-02/` (기존 PDCA 기록)

---

## 2. Scope

### 2.1 In Scope

- [ ] Gemini 스타일 채팅 UI 페이지 (`/consultation`)
- [ ] 좌측 사이드바: 대화 기록 목록 (세션별 그룹핑)
- [ ] 채팅 영역: 사용자 질문 입력 + AI 답변 표시
- [ ] 전문가 검증 신청 기능 (답변별 "전문가 검증" 버튼)
- [ ] 전문가 검증 완료 상태 표시 (배지/라벨)
- [ ] 새 채팅 시작 기능
- [ ] 반응형 디자인 (모바일/데스크톱)

### 2.2 Out of Scope

- 실제 LLM API 연동 (mock 데이터로 시뮬레이션, API 키 연동은 별도 feature)
- 전문가 매칭/배정 시스템 (관리자 백오피스)
- 결제/과금 시스템
- 사용자 인증/로그인 (별도 feature)
- 채팅 데이터 DB 저장 (로컬 상태 관리로 구현)

---

## 3. Requirements

### 3.1 Functional Requirements

| ID | Requirement | Priority | Status |
|----|-------------|----------|--------|
| FR-01 | Gemini 스타일 채팅 메인 화면 - 중앙에 입력창, 환영 메시지 표시 | High | Pending |
| FR-02 | 채팅 입력창 - 텍스트 입력, Enter/버튼으로 전송 | High | Pending |
| FR-03 | AI 답변 표시 - 마크다운 렌더링, 타이핑 효과(선택) | High | Pending |
| FR-04 | 좌측 사이드바 - 대화 기록 목록 (제목, 날짜별 그룹) | High | Pending |
| FR-05 | 사이드바 토글 - 햄버거 메뉴로 사이드바 열기/닫기 | Medium | Pending |
| FR-06 | 새 채팅 시작 - 사이드바 상단 "새 채팅" 버튼 | High | Pending |
| FR-07 | 전문가 검증 신청 - AI 답변에 "전문가 검증" 버튼 표시 | High | Pending |
| FR-08 | 전문가 검증 완료 표시 - 검증된 답변에 "전문가 검증 완료" 배지 | High | Pending |
| FR-09 | 대화 기록 클릭 시 해당 대화 복원 | Medium | Pending |
| FR-10 | 추천 질문 칩 - 초기 화면에 세무/회계/경리 관련 추천 질문 표시 | Medium | Pending |

### 3.2 Non-Functional Requirements

| Category | Criteria | Measurement Method |
|----------|----------|-------------------|
| Performance | 페이지 로드 < 2s, 채팅 응답 표시 < 500ms (mock) | Lighthouse, 체감 테스트 |
| Responsive | 모바일(360px) ~ 데스크톱(1920px) 완벽 대응 | 브라우저 리사이즈 테스트 |
| Accessibility | 키보드 네비게이션, 적절한 aria 라벨 | 수동 검증 |

---

## 4. Success Criteria

### 4.1 Definition of Done

- [ ] 모든 Functional Requirements 구현 완료
- [ ] Gemini UI와 시각적 유사도 80% 이상
- [ ] 반응형 동작 확인 (모바일/데스크톱)
- [ ] 전문가 검증 플로우 정상 작동
- [ ] 기존 BIZSCHOOL 디자인 시스템과 일관성 유지

### 4.2 Quality Criteria

- [ ] TypeScript 타입 에러 없음
- [ ] ESLint 에러 없음
- [ ] 빌드 성공

---

## 5. Risks and Mitigation

| Risk | Impact | Likelihood | Mitigation |
|------|--------|------------|------------|
| Gemini UI 복잡도로 구현 시간 초과 | Medium | Medium | 핵심 기능 우선 구현, 사이드바 애니메이션 등은 2차로 |
| 채팅 상태 관리 복잡성 | Medium | Medium | useState/useReducer로 단순하게 시작, 필요 시 Zustand 도입 |
| 기존 레이아웃(Header/Footer)과 충돌 | Low | Medium | 상담 페이지 전용 레이아웃 적용 |

---

## 6. Architecture Considerations

### 6.1 Project Level Selection

| Level | Characteristics | Recommended For | Selected |
|-------|-----------------|-----------------|:--------:|
| **Starter** | Simple structure (`components/`, `lib/`, `types/`) | Static sites, portfolios, landing pages | ✅ |
| **Dynamic** | Feature-based modules, BaaS integration | Web apps with backend, SaaS MVPs | ☐ |
| **Enterprise** | Strict layer separation, DI, microservices | High-traffic systems | ☐ |

> Starter 레벨 유지 - 현재 프로젝트 구조와 일관성. LLM API 연동 시 Dynamic으로 전환 가능.

### 6.2 Key Architectural Decisions

| Decision | Options | Selected | Rationale |
|----------|---------|----------|-----------|
| Framework | Next.js (기존) | Next.js 16 | 프로젝트 일관성 |
| State Management | useState / useReducer / Zustand | useState + useReducer | 채팅 상태 관리에 적합, 외부 의존성 없음 |
| Styling | Tailwind CSS v4 (기존) | Tailwind CSS v4 | 프로젝트 일관성 |
| 아이콘 | lucide-react (기존) | lucide-react | 프로젝트 일관성 |
| 레이아웃 | 기존 공유 / 전용 | 전용 레이아웃 | Gemini처럼 Header/Footer 없는 전체 화면 채팅 UI |
| 마크다운 렌더링 | react-markdown / 직접 구현 | 직접 구현 (간단한 포맷) | 의존성 최소화, 필요 시 추가 |

### 6.3 Clean Architecture Approach

```
Selected Level: Starter

폴더 구조:
src/
├── app/
│   └── consultation/
│       ├── layout.tsx          ← 전용 레이아웃 (Header/Footer 제외)
│       └── page.tsx            ← 메인 페이지
├── components/
│   └── consultation/
│       ├── Sidebar.tsx         ← 좌측 대화 기록 패널
│       ├── ChatArea.tsx        ← 채팅 메인 영역
│       ├── ChatMessage.tsx     ← 개별 메시지 (사용자/AI)
│       ├── ChatInput.tsx       ← 입력창
│       ├── WelcomeScreen.tsx   ← 초기 환영 화면
│       ├── ExpertBadge.tsx     ← 전문가 검증 완료 배지
│       └── SuggestChips.tsx    ← 추천 질문 칩
├── types/
│   └── index.ts                ← 기존 파일에 타입 추가
└── data/
    └── consultation.ts         ← mock 데이터 (추천 질문, 샘플 답변)
```

### 6.4 UI 레이아웃 설계 (Gemini 참고)

```
┌──────────────────────────────────────────────────────────┐
│ ☰  BIZSCHOOL AI 상담        [새 채팅]                    │
├──────────┬───────────────────────────────────────────────┤
│          │                                               │
│ 대화기록  │          채팅 영역                              │
│          │                                               │
│ ───────  │  ┌─────────────────────────────────────────┐  │
│ 📄 세무   │  │ 🤖 AI 답변 내용...                       │  │
│   상담    │  │                                         │  │
│ 📄 회계   │  │    [전문가 검증] [복사] [👍] [👎]         │  │
│   질문    │  └─────────────────────────────────────────┘  │
│ 📄 경리   │                                               │
│   문의    │  ┌─────────────────────────────────────────┐  │
│          │  │ 🤖 AI 답변 (전문가 검증 완료 ✅)           │  │
│ ───────  │  │                                         │  │
│ 설정     │  │    [전문가 검증 완료 ✅] [복사] [👍] [👎]  │  │
│          │  └─────────────────────────────────────────┘  │
│          │                                               │
│          │  ┌───────────────────────────────────────┐    │
│          │  │ 메시지를 입력하세요...          [전송]  │    │
│          │  └───────────────────────────────────────┘    │
├──────────┴───────────────────────────────────────────────┤
│ [세무 상담] [회계 질문] [경리 문의] [세금 계산]            │
└──────────────────────────────────────────────────────────┘

초기 화면 (대화 없을 때):
┌──────────┬───────────────────────────────────────────────┐
│          │                                               │
│ (비어있음) │     💬 AI 전문가에게 물어보세요               │
│          │                                               │
│          │     세무, 회계, 경리 관련 궁금한 점을            │
│          │     자유롭게 질문해 주세요.                      │
│          │                                               │
│          │  ┌───────────────────────────────────────┐    │
│          │  │ 메시지를 입력하세요...          [전송]  │    │
│          │  └───────────────────────────────────────┘    │
│          │                                               │
│          │  [세무 상담] [회계 질문] [경리 문의] [세금 계산] │
│          │                                               │
└──────────┴───────────────────────────────────────────────┘
```

---

## 7. Convention Prerequisites

### 7.1 Existing Project Conventions

- [x] ESLint 설정 (`eslint.config.mjs`)
- [x] TypeScript 설정 (`tsconfig.json`)
- [x] Tailwind CSS v4 + PostCSS 설정
- [x] CSS 변수 기반 디자인 토큰 (`globals.css`)
- [ ] `CLAUDE.md` 코딩 컨벤션 섹션 없음

### 7.2 Conventions to Define/Verify

| Category | Current State | To Define | Priority |
|----------|---------------|-----------|:--------:|
| **Naming** | PascalCase 컴포넌트, camelCase 변수 | 유지 | High |
| **Folder structure** | `components/{domain}/` 패턴 확립 | `consultation/` 추가 | High |
| **CSS Variables** | `var()` 래퍼 필수 (Tailwind v4) | 유지 | High |
| **Client/Server** | 인터랙티브 컴포넌트만 "use client" | 유지 | Medium |

### 7.3 Environment Variables Needed

| Variable | Purpose | Scope | To Be Created |
|----------|---------|-------|:-------------:|
| (없음 - mock 데이터 사용) | - | - | - |

> 향후 LLM API 연동 시: `NEXT_PUBLIC_LLM_API_URL`, `LLM_API_KEY` 등 필요

### 7.4 Pipeline Integration

| Phase | Status | Document Location | Command |
|-------|:------:|-------------------|---------|
| Phase 1 (Schema) | ☐ | 필요 시 추가 | - |
| Phase 2 (Convention) | ☐ | 기존 패턴 따름 | - |

---

## 8. 핵심 데이터 타입 (Preview)

```typescript
// 대화 세션
interface ConsultationSession {
  id: string;
  title: string;          // 첫 질문 요약 (사이드바 표시용)
  createdAt: string;
  updatedAt: string;
  messages: ChatMessage[];
}

// 채팅 메시지
interface ChatMessage {
  id: string;
  role: 'user' | 'assistant';
  content: string;
  createdAt: string;
  expertVerification?: ExpertVerification;
}

// 전문가 검증
interface ExpertVerification {
  status: 'pending' | 'verified' | 'rejected';
  expertName?: string;
  verifiedAt?: string;
  comment?: string;
}
```

---

## 9. Next Steps

1. [ ] Design 문서 작성 (`ai-expert-consultation.design.md`)
2. [ ] 컴포넌트 상세 설계 및 상태 관리 설계
3. [ ] 구현 시작

---

## Version History

| Version | Date | Changes | Author |
|---------|------|---------|--------|
| 0.1 | 2026-02-26 | Initial draft | Allen |
