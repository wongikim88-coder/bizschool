# Design: 도서 페이지 (book-page)

> Feature: 도서 목록 페이지 구현
> Created: 2026-02-26
> Status: Design
> Plan Reference: `docs/01-plan/features/book-page.plan.md`

---

## 1. 아키텍처 개요

```
/books (page.tsx - Server Component)
├── BookBanner           - 페이지 상단 배너 (Server)
├── CategoryFilter       - 카테고리 필터 탭 (Client)
├── BookSearch           - 도서 검색바 (Client)
├── BookListCard (x20)   - 가로형 도서 카드 리스트 (Server)
└── Pagination           - 페이지네이션 (Client)
```

### 1.1 데이터 흐름

```
URL Query Params (?page=1&category=all&search=)
         ↓
  /books/page.tsx (Server Component)
    - searchParams에서 page, category, search 추출
    - allBooks 데이터에서 필터링/검색/슬라이싱
         ↓
  각 컴포넌트에 props 전달
```

- **Server Component** (`page.tsx`): URL searchParams로 필터/페이지 상태 관리, 데이터 필터링 로직 수행
- **Client Components** (`CategoryFilter`, `BookSearch`, `Pagination`): URL을 변경하여 페이지 이동/필터링 트리거

## 2. 데이터 모델 상세

### 2.1 Book 타입 확장

```typescript
// src/types/index.ts
export interface Book {
  id: string;
  title: string;
  author: string;
  cover: string;
  price: number;
  originalPrice?: number;
  discountRate?: number;
  rating: number;
  reviewCount: number;
  badges?: Badge[];
  // 신규 필드
  publisher?: string;      // 출판사
  publishDate?: string;    // 발행일 (예: "2026년 02월 10일")
  description?: string;    // 도서 소개 텍스트
  category?: string;       // 카테고리 키 (예: "경영전략")
  isSoldOut?: boolean;     // 품절 여부
}
```

### 2.2 카테고리 정의

```typescript
// src/data/books.ts에 추가
export const bookCategories = [
  { key: "all", label: "전체" },
  { key: "경영전략", label: "경영전략" },
  { key: "마케팅", label: "마케팅" },
  { key: "재무회계", label: "재무회계" },
  { key: "자기계발", label: "자기계발" },
  { key: "리더십", label: "리더십" },
  { key: "IT기술", label: "IT/기술" },
] as const;
```

### 2.3 상수

```typescript
export const BOOKS_PER_PAGE = 20;
```

## 3. 컴포넌트 상세 설계

### 3.1 `/books/page.tsx` (Server Component)

**역할**: 도서 목록 페이지의 루트. URL searchParams를 파싱하여 데이터 필터링 및 페이지네이션 로직 수행.

```typescript
// Props: Next.js App Router searchParams
interface BooksPageProps {
  searchParams: Promise<{
    page?: string;
    category?: string;
    search?: string;
  }>;
}
```

**데이터 처리 로직**:
1. `category` 파라미터로 도서 필터링 (기본: "all")
2. `search` 파라미터로 제목/저자 검색 필터링
3. `page` 파라미터로 현재 페이지 결정 (기본: 1)
4. `BOOKS_PER_PAGE`(20)으로 슬라이싱
5. 총 페이지 수 계산: `Math.ceil(filteredBooks.length / BOOKS_PER_PAGE)`

**레이아웃 구조**:
```
<div className="mx-auto max-w-[1200px] px-4">
  <BookBanner />
  <CategoryFilter currentCategory={category} />
  <BookSearch currentSearch={search} />
  <div className="도서 리스트 영역">
    {paginatedBooks.map(book => <BookListCard book={book} />)}
  </div>
  <Pagination currentPage={page} totalPages={totalPages} />
</div>
```

---

### 3.2 `BookBanner.tsx` (Server Component)

**역할**: 페이지 상단 배너. dzbizschool의 "ETAX BOOKS is Good contents" 영역을 참조.

**디자인 사양**:
- 높이: `py-12 md:py-16`
- 배경: `bg-gradient-to-r from-[var(--color-dark-navy)] to-[var(--color-dark-deep)]`
- 제목: "BIZSCHOOL BOOKS" (흰색, 2xl~3xl 볼드)
- 부제목: "비즈니스 성장을 위한 전문 도서를 만나보세요" (흰색/70% 투명도)
- 둥근 모서리: `rounded-2xl` (기존 MainBanner와 동일)

```
┌──────────────────────────────────────────────────────┐
│                                                      │
│   BIZSCHOOL BOOKS                                    │
│   비즈니스 성장을 위한 전문 도서를 만나보세요         │
│                                                      │
└──────────────────────────────────────────────────────┘
```

---

### 3.3 `CategoryFilter.tsx` (Client Component)

**역할**: 카테고리별 필터 탭. "전체 | 경영전략 | 마케팅 | ..." 형태.

**Props**:
```typescript
interface CategoryFilterProps {
  currentCategory: string;  // 현재 선택된 카테고리
}
```

**동작**:
- 탭 클릭 시 `router.push(/books?category={key}&page=1)` (페이지를 1로 리셋)
- 기존 search 파라미터 유지

**디자인 사양**:
- 가로 스크롤 가능한 탭 리스트 (모바일에서 `overflow-x-auto`)
- 활성 탭: `bg-[var(--color-primary)] text-white rounded-full`
- 비활성 탭: `border border-[var(--color-border)] text-[var(--color-body)] rounded-full`
- 간격: `gap-2`, 패딩: `px-4 py-1.5`

```
[ 전체 ] [ 경영전략 ] [ 마케팅 ] [ 재무회계 ] [ 자기계발 ] [ 리더십 ] [ IT/기술 ]
  ↑ 활성(파란배경)     ↑ 비활성(회색테두리)
```

---

### 3.4 `BookSearch.tsx` (Client Component)

**역할**: 도서명/저자 검색바.

**Props**:
```typescript
interface BookSearchProps {
  currentSearch: string;  // 현재 검색어
}
```

**동작**:
- 검색 입력 후 Enter 또는 검색 버튼 클릭 시 `router.push(/books?search={value}&page=1)`
- 기존 category 파라미터 유지
- 빈 검색어 시 search 파라미터 제거

**디자인 사양**:
- 입력 필드: `border border-[var(--color-border)] rounded-lg px-4 py-2`
- 검색 버튼: `bg-[var(--color-dark)] text-white rounded-lg px-4 py-2`
- 플레이스홀더: "도서명 또는 저자를 검색하세요"
- 아이콘: `Search` (lucide-react)
- 너비: `max-w-md`

```
┌────────────────────────────────────┐ ┌───────┐
│ 🔍 도서명 또는 저자를 검색하세요   │ │ 검색  │
└────────────────────────────────────┘ └───────┘
```

---

### 3.5 `BookListCard.tsx` (Server Component)

**역할**: 가로형 도서 카드. dzbizschool의 도서 리스트 아이템을 참조.

**Props**:
```typescript
interface BookListCardProps {
  book: Book;
}
```

**레이아웃** (Desktop):
```
┌──────────────────────────────────────────────────────────────────┐
│ ┌────────────┐  제목 (텍스트 xl 볼드)                            │
│ │            │  저자 | 출판사 | 발행일  (muted 색상)             │
│ │  책 표지   │  정가 : 100,000원 (취소선)                        │
│ │  180x240   │  할인가 : 90,000원 (빨간색, 10%할인)   [장바구니] │
│ │            │  ┌──────────────────────────────┐      [바로구매] │
│ │            │  │ 책 소개 텍스트 (회색 박스)   │                 │
│ └────────────┘  └──────────────────────────────┘                 │
└──────────────────────────────────────────────────────────────────┘
```

**레이아웃** (Mobile):
```
┌──────────────────────────────────┐
│ ┌──────────┐ 제목 (볼드)         │
│ │ 책 표지  │ 저자 | 출판사       │
│ │ 120x160  │ 정가 / 할인가       │
│ └──────────┘                     │
│ 책 소개 텍스트                    │
│ [장바구니] [바로구매]             │
└──────────────────────────────────┘
```

**디자인 사양**:

| 요소 | 데스크톱 | 모바일 |
|------|----------|--------|
| 컨테이너 | `flex gap-6 p-6` | `flex gap-4 flex-wrap p-4` |
| 표지 이미지 | `w-[180px] h-[240px]` | `w-[120px] h-[160px]` |
| 구분선 | `border-b border-[var(--color-border)]` | 동일 |
| 제목 | `text-xl font-bold text-[var(--color-dark)]` | `text-base font-bold` |
| 메타 정보 | `text-sm text-[var(--color-muted)]` | 동일 |
| 정가 | `text-sm text-[var(--color-muted)] line-through` | 동일 |
| 할인가 | `text-lg font-bold text-[var(--color-red)]` | `text-base` |
| 설명 박스 | `bg-[var(--color-light-bg)] p-4 rounded-lg text-sm` | 동일 |
| 장바구니 버튼 | `bg-[var(--color-primary)] text-white rounded-lg px-6 py-2.5` | 풀너비 |
| 바로구매 버튼 | `bg-emerald-600 text-white rounded-lg px-6 py-2.5` | 풀너비 |
| 품절 표시 | 제목 옆 `[품절]` `text-[var(--color-red)] font-bold` | 동일 |

**이미지 처리**:
- 실제 이미지가 없으므로 기존 BookCard와 동일하게 그라데이션 플레이스홀더 사용
- `bg-gradient-to-b from-gray-200 to-gray-300` + 제목/저자 텍스트
- `rounded-lg overflow-hidden shadow-sm`

---

### 3.6 `Pagination.tsx` (Client Component)

**역할**: 페이지 번호 기반 페이지 이동.

**Props**:
```typescript
interface PaginationProps {
  currentPage: number;
  totalPages: number;
}
```

**동작**:
- 번호 클릭 시 `router.push(/books?page={n})` (기존 category, search 유지)
- 이전(`<`) / 다음(`>`) 화살표 버튼
- 첫 페이지에서 이전 버튼 비활성화, 마지막 페이지에서 다음 버튼 비활성화

**페이지 번호 표시 로직**:
- 최대 5개 번호 표시
- 현재 페이지를 중심으로 좌우 배치
- 예시: 현재 4페이지 → `< 2 3 [4] 5 6 >`
- 예시: 현재 1페이지 → `[1] 2 3 4 5 >`
- 예시: 현재 마지막(8) → `< 4 5 6 7 [8]`

**디자인 사양**:
```
< | 1 | 2 | [3] | 4 | 5 | >
```

| 요소 | 스타일 |
|------|--------|
| 컨테이너 | `flex items-center justify-center gap-1 py-8` |
| 현재 페이지 | `bg-[var(--color-primary)] text-white w-10 h-10 rounded-lg font-bold` |
| 다른 페이지 | `text-[var(--color-body)] w-10 h-10 rounded-lg hover:bg-[var(--color-light-bg)]` |
| 화살표 | `text-[var(--color-muted)] w-10 h-10 rounded-lg hover:bg-[var(--color-light-bg)]` |
| 비활성 화살표 | `text-gray-300 cursor-not-allowed` |

---

## 4. 데이터 파일 설계

### 4.1 `src/data/books.ts` 확장

기존 8권의 `sampleBooks` 배열을 유지하면서, 도서 목록 페이지용 `allBooks` 배열(40권 이상)을 추가.

```typescript
// 기존 sampleBooks는 메인 페이지에서 계속 사용
export const sampleBooks: Book[] = [ /* 기존 8권 유지 */ ];

// 도서 목록 페이지용 전체 데이터
export const allBooks: Book[] = [
  // 40권 이상, category/publisher/publishDate/description 포함
  // 카테고리별 고르게 분포 (5~7권씩)
  // 일부 품절(isSoldOut: true) 포함
  // 일부 할인 없는 도서 포함
];

export const bookCategories = [ /* 카테고리 목록 */ ];
export const BOOKS_PER_PAGE = 20;
```

**데이터 분포 목표** (총 42권):

| 카테고리 | 권수 |
|----------|------|
| 경영전략 | 7 |
| 마케팅 | 6 |
| 재무회계 | 7 |
| 자기계발 | 7 |
| 리더십 | 8 |
| IT/기술 | 7 |

- 할인 도서: 약 70% (30권)
- 품절 도서: 약 5% (2권)

## 5. URL 라우팅 설계

### 5.1 URL 파라미터

| 파라미터 | 타입 | 기본값 | 예시 |
|----------|------|--------|------|
| `page` | number | 1 | `?page=2` |
| `category` | string | "all" | `?category=마케팅` |
| `search` | string | "" | `?search=스타트업` |

### 5.2 URL 예시

```
/books                           → 전체 도서, 1페이지
/books?page=2                    → 전체 도서, 2페이지
/books?category=마케팅            → 마케팅 도서, 1페이지
/books?category=마케팅&page=2     → 마케팅 도서, 2페이지
/books?search=린스타트업          → 검색 결과, 1페이지
/books?category=경영전략&search=전략&page=1 → 경영전략 + 검색
```

### 5.3 파라미터 변경 시 동작

| 액션 | page 리셋 | category 유지 | search 유지 |
|------|-----------|---------------|-------------|
| 카테고리 변경 | 1로 리셋 | 변경됨 | 유지 |
| 검색 실행 | 1로 리셋 | 유지 | 변경됨 |
| 페이지 이동 | 변경됨 | 유지 | 유지 |

## 6. 구현 순서 (Implementation Order)

| # | 파일 | 컴포넌트 | 의존성 |
|---|------|----------|--------|
| 1 | `src/types/index.ts` | Book 타입 확장 | 없음 |
| 2 | `src/data/books.ts` | allBooks 데이터 + 카테고리 + 상수 | #1 |
| 3 | `src/components/books/Pagination.tsx` | 페이지네이션 | 없음 |
| 4 | `src/components/books/BookListCard.tsx` | 가로형 도서 카드 | #1 |
| 5 | `src/components/books/CategoryFilter.tsx` | 카테고리 필터 | #2 |
| 6 | `src/components/books/BookSearch.tsx` | 검색바 | 없음 |
| 7 | `src/components/books/BookBanner.tsx` | 배너 | 없음 |
| 8 | `src/app/books/page.tsx` | 도서 목록 페이지 | #2~#7 전부 |

## 7. 디자인 토큰 매핑

기존 `globals.css`의 CSS 변수를 그대로 활용:

| 용도 | CSS Variable | 값 |
|------|--------------|------|
| 배너 배경 | `--color-dark-navy` | `#101828` |
| 배너 배경2 | `--color-dark-deep` | `#1e2839` |
| 제목 색상 | `--color-dark` | `#282c34` |
| 본문 색상 | `--color-body` | `#4a5565` |
| 보조 색상 | `--color-muted` | `#6a7282` |
| 테두리 | `--color-border` | `#e5e7eb` |
| 배경 (설명박스) | `--color-light-bg` | `#f8f9fa` |
| 할인가/품절 | `--color-red` | `#fa5252` |
| 활성 탭/버튼 | `--color-primary` | `#155dfc` |

**Tailwind CSS v4 주의**: 반드시 `var()` 래퍼 사용
- `bg-[var(--color-dark-navy)]` (O)
- `bg-[--color-dark-navy]` (X - 투명하게 렌더링됨)

## 8. 반응형 브레이크포인트

| 브레이크포인트 | 레이아웃 변경 |
|----------------|---------------|
| `< 640px` (모바일) | 도서 카드 세로 배치, 버튼 풀너비, 검색바 풀너비 |
| `640px ~ 1024px` (태블릿) | 도서 카드 가로형 유지, 표지 축소 |
| `> 1024px` (데스크톱) | 전체 가로형 레이아웃, 버튼 우측 정렬 |

## 9. 접근성 요구사항

- 페이지네이션: `aria-label="페이지 이동"`, 현재 페이지 `aria-current="page"`
- 카테고리 필터: `role="tablist"`, 각 탭 `role="tab"`, `aria-selected`
- 검색: `<form>` 태그, `aria-label="도서 검색"`
- 품절 도서: `aria-label`에 품절 상태 포함
- 장바구니/바로구매 버튼: 도서명 포함 `aria-label`
